import { CalculationRequest } from './calculation-request';

describe('CalculationRequest', () => {
  it('should create an instance', () => {
    expect(new CalculationRequest()).toBeTruthy();
  });
});
