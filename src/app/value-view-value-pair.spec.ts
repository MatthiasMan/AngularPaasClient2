import { ValueViewValuePair } from './value-view-value-pair';

describe('ValueViewValuePair', () => {
  it('should create an instance', () => {
    expect(new ValueViewValuePair()).toBeTruthy();
  });
});
